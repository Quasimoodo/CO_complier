package compiler;
import compiler.analyzer.*;
import compiler.tokenizer.*;

public class App
{
    public static void main(String[] args) throws Exception
    {
        Tokenizer ter =new Tokenizer(args[0]);
      //  Objectfile obj=new 
     //   Analyzer aer = new Analyzer(ter);
    /*    try
        {
            aer.analyse();

        }
        */
        throw new Exception();
    }
}
